fun abs(Num)
    var Tmp = []
    if Num >= 0 then
        return Num    
    end

    for i = Num to 0 then
        append(Tmp, i)
    end

    return len(Tmp)
end

fun sqr(Num)
    return Num/2
end